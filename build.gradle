buildscript {
    ext {
        compose_ui_version = '1.2.0-alpha05'
    }
    dependencies {
        // Realm
        //classpath 'io.realm:realm-gradle-plugin:10.10.1'
        // Hilt
        classpath 'com.google.dagger:hilt-android-gradle-plugin:2.40.5'
    }
}// Top-level build file where you can add configuration options common to all sub-projects/modules.
plugins {
    id 'com.android.application' version '7.3.0-alpha07' apply false
    id 'com.android.library' version '7.3.0-alpha07' apply false
    id 'org.jetbrains.kotlin.android' version '1.6.10' apply false

    // My Library
    // Hilt
    //id 'com.google.dagger:hilt-android-gradle-plugin' version '2.40.5' apply false
}